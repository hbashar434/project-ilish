import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Blog = () => {
    // const blogData=useLoaderData();

    return (
        <div>
            <h1>Blog.</h1>
        </div>
    );
};

export default Blog;